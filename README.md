id="n3i9dw"
# NAT Remedy Finder Premium

Deploy on Vercel:

1. Create a new Vercel project from this folder.
2. Add environment variable `OPENAI_API_KEY`.
3. Deploy.

The frontend is `index.html` and the serverless OpenAI endpoint is `api/remedy.js`.
